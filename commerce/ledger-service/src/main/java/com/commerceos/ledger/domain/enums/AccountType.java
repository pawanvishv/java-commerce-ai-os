package com.commerceos.ledger.domain.enums;

public enum AccountType {
    RECEIVABLE,
    GST_COLLECTED,
    REVENUE,
    GST_PAYABLE,
    COMMISSION_REVENUE,
    PLATFORM_GST_PAYABLE,
    TCS_PAYABLE,
    SELLER_NET_PAYABLE
}
