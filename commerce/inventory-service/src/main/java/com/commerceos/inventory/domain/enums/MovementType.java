package com.commerceos.inventory.domain.enums;

public enum MovementType {
    PURCHASE_RECEIPT,
    SALE,
    RESERVATION,
    RESERVATION_RELEASE,
    RESERVATION_CONFIRM,
    ADJUSTMENT,
    DAMAGE,
    EXPIRY,
    TRANSFER_OUT,
    TRANSFER_IN,
    RETURN
}
